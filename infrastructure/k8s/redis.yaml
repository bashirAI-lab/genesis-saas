apiVersion: apps/v1
kind: Deployment
metadata:
  name: saas-redis
  namespace: saas-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: saas-redis
  template:
    metadata:
      labels:
        app: saas-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: saas-redis
  namespace: saas-platform
spec:
  selector:
    app: saas-redis
  ports:
    - port: 6379
      targetPort: 6379
